# rag-platform — variáveis de ambiente de exemplo
#
# Este arquivo é apenas um placeholder criado em RAG-001 (inicialização do
# repositório). A validação formal de configuração via Pydantic Settings,
# com a lista completa de variáveis obrigatórias/opcionais, é entregue em
# RAG-004. Nunca copie valores reais de credenciais para este arquivo.

# Identificação do ambiente de execução (local | development | production).
ENVIRONMENT=local
